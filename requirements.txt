# CS2 Heightmap Generator Requirements
# Python 3.8+

# Core image processing
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.10.0

# Procedural noise generation
# Note: Using perlin-noise instead of noise (no C++ compiler needed)
perlin-noise>=1.12
opensimplex>=0.4.5

# Performance optimization (optional but recommended)
# Provides 10-100x speedup via C++/Cython implementation
pyfastnoiselite>=0.0.6

# Progress tracking
tqdm>=4.65.0

# Optional: GUI support (uncomment if needed)
# PyQt5>=5.15.0

# Optional: Real-world elevation data (uncomment if needed)
# elevation>=1.1.3
# srtm.py>=0.3.7
