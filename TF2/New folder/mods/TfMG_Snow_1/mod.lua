function data()
return {
	info = {
		minorVersion = 0,
		severityAdd = "NONE",
		severityRemove = "NONE",
		name = _("TfMG snow"),
		description = _("Just adds snow on top of the mountains."),
	},
	runFn = function (settings)
	end
}
end
